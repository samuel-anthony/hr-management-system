<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eims">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_api_keys" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity android:name=".HRProjectMemberEdit" />
        <activity android:name=".TaskProject" />
        <activity android:name=".Profile" />
        <activity android:name=".AdminClaim" />
        <activity android:name=".AdminDetail" />
        <activity android:name=".AdminLeave" />
        <activity android:name=".AdminAttendance" />
        <activity android:name=".HRProjectAddEdit" />
        <activity android:name=".HRProjectMain" />
        <activity android:name=".HREmployeeEdit" />
        <activity android:name=".HREmployeeEditProject" />
        <activity android:name=".HREmployeeEditLeave" />
        <activity android:name=".HREmployeeAdd" />
        <activity android:name=".HREmployeeMain" />
        <activity android:name=".TaskDetail" />
        <activity android:name=".TaskSearch" />
        <activity android:name=".TaskMain" />
        <activity android:name=".AdminMain" />
        <activity android:name=".Report_Menu" />
        <activity android:name=".Report_Main" />
        <activity android:name=".Reimbursement" />
        <activity android:name=".Leave" />
        <activity android:name=".Attendance" />
        <activity android:name=".splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>